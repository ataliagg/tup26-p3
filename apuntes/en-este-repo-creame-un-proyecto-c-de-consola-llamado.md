# Como pedir a Codex

```cs
foreach(var x in "uno dos tres".Split(" ")){
	Console.WriteLine(x)
}
```

- [ ] 1.uno
2.dos
3.tres

En este repo, creame un proyecto C# de consola llamado `editcsv` con una TUI para editar archivos CSV. Quiero poder navegar y editar celdas, agregar y borrar filas y columnas, renombrar la columna actual, ordenar por la columna actual, cambiar el delimitador, alinear a la derecha las columnas numéricas y alternar con `.` entre mostrar enteros o 2 decimales solo para la columna actual. Dejalo compilando y probado.


Desarrollá en este repo un programa `editcsv` en C# (.NET consola) con TUI para editar CSV. Implementalo directamente. Debe abrir un CSV, permitir navegar y editar celdas, agregar/borrar filas y columnas, renombrar la columna actual, ordenar por la columna actual, cambiar el delimitador, mostrar columnas numéricas alineadas a la derecha y alternar con `.` la visualización de enteros/2 decimales para la columna actual. Verificá que compile y funcione.
